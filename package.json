{
  "name": "techstore",
  "private": true,
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run start:dev",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "lb:up": "docker compose -f docker-compose.load-balancing.yml up --build",
    "lb:down": "docker compose -f docker-compose.load-balancing.yml down -v"
  }
}
